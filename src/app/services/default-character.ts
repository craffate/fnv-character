import { Character } from "@classes/character";
import { DEFAULT_ACTORVALUES } from "./default-actor-values";

export const DEFAULT_CHARACTER: Character = new Character("Courier", 0, DEFAULT_ACTORVALUES, []);
